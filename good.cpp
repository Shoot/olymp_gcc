#include <bits/stdc++.h>
using namespace std;

signed main() {
    int n, W;
    cin >> n >> W;
    for (int i = 0; i < n; i += 1) {
        int a, b;
        cin >> a >> b;
    }
    // Sum(a_1, a_2, ..,  a_k) * Max(b_1, b_2, .., b_k) \leq W
    // maximal subset
}