#include <iostream>
#include <vector>
#include <numeric>
#include <algorithm>
#include <map>
using namespace std;
signed main() {
    int tp;
    cin >> tp;
    if (tp == 0) {
        int len, su, order;
        cin >> len >> su >> order;
        int curr_su = 0;
        auto gen = [&] (int idx) -> void {
            for (int nxt = )
        };
    } else {
        int sz;
        cin >> sz;
        for (int i = 0; i < sz; i += 1) {

        }
    }
}